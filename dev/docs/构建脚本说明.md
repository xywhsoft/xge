# XGE 构建脚本说明

本文档记录 XGE V2 当前阶段的构建脚本约定。项目暂不使用 CMake，优先保留轻量、可读、可直接调试的脚本。

## 基本约定

- Windows 使用 `*.bat`。
- Linux/macOS/类 Unix 使用 `*.sh`。
- 脚本命名格式为 `build_<target>.bat` / `build_<target>.sh`。
- 编译输出统一放入 `build/` 目录。
- 引擎源码入口统一为根目录 `xge.c`。
- 公开头为根目录 `xge.h`。
- 示例源码放在 `examples/<name>/main.c`。
- 测试源码放在 `test/`。
- 第三方依赖放在 `lib/`。

## 环境要求

Windows:

- 需要 `gcc` 位于 `PATH` 中。
- 当前脚本使用 MinGW 风格链接参数。
- 默认链接库包括 `ws2_32`、`iphlpapi`、`gdi32`、`user32`、`shell32`、`opengl32`、`ole32`、`winmm`、`avrt`。

Linux/macOS/类 Unix:

- 需要 `cc` 可用。
- 默认链接 `-lm -ldl -pthread`。
- 平台图形依赖会随着 Sokol/EGL/平台后端完善继续补充。

## 核心脚本

- `build_test.*`：编译并运行纯 C 测试程序。
- `build_dll.*`：编译动态库输出。
- `build_exe.*`：编译最小 MVP 示例程序。
- `build_platform_smoke_exe.*`：编译通用平台冒烟示例，输出 caps 并绘制基础 shape/texture。
- `run_platform_smoke.*`：运行通用 platform smoke 示例，自动退出并检查日志。
- `check_platform_smoke.bat`：Windows 下构建、运行并检查通用 platform smoke。
- `check_platform_backends.*`：检查当前机器是否具备常见平台后端工具链。
- `check_platform_scaffold.*`：检查 Android/iOS/Web/Linux/macOS 平台 scaffold 文件和关键配置是否完整。
- `check_platform_results.*`：读取 `dev/docs/平台后端验证结果.md`，汇总哪些平台任务可关闭。
- `check_platform_smoke_log.*`：检查 platform smoke 输出日志是否包含 caps 和 runtime 快照。
- `check_linux_x11_smoke.sh`：Linux X11 下构建、运行并检查 Sokol/GLCore platform smoke。
- `check_linux_egl_smoke.sh`：Linux X11/EGL 下构建、运行并检查 Sokol/GLES3 platform smoke。
- `check_macos_smoke.sh`：macOS 下构建、运行并检查 Sokol/OpenGL platform smoke。
- `check_platform_all.*`：串行执行 scaffold、工具链和结果状态检查。
- `check_script_docs.*`：检查文档中引用的脚本是否真实存在。
- `check_docs.*`：检查正式文档链接，计划中文档只给警告，真实断链会失败。
- `record_platform_result.*`：把目标平台冒烟结果写入 `dev/docs/平台后端验证结果.md`。
- `build_egl_surfaceless_exe.sh`：在目标 Linux 板卡上编译 EGL surfaceless 冒烟示例。
- `check_board_linux_egl.sh`：在目标 Linux 板卡上顺序构建并运行 pbuffer 与 surfaceless EGL 冒烟示例。
- `build_macos_exe.sh`：在 macOS 上使用 Sokol + OpenGL 编译 MVP 示例。
- `build_android_ndk.*`：使用 Android NDK/CMake 编译 NativeActivity scaffold。
- `build_android_apk.*`：使用 `platform/android/gradle` Gradle scaffold 编译 Android debug APK。
- `run_android_apk.*`：使用 `adb` 安装并启动 Android debug APK。
- `build_ios_sim_exe.sh`：在 macOS 上使用 Xcode 命令行工具编译 iOS simulator MVP app bundle。
- `run_ios_sim_exe.sh`：使用 `xcrun simctl` 安装并启动 iOS simulator app bundle。
- `build_linux_x11_exe.sh`：在 Linux X11 上使用 Sokol + OpenGL 编译 platform smoke 示例。
- `build_linux_egl_exe.sh`：在 Linux X11 上使用 Sokol + EGL/GLES3 编译 MVP 示例。
- `build_web_exe.*`：使用 Emscripten 编译 Web/WebGL2 MVP 示例，并使用 `platform/web/shell.html` 作为浏览器 shell。
- `serve_web_exe.*`：启动本地 HTTP server 运行 Web/Emscripten 构建产物。
- `build_board_linux_egl_exe.sh`：在目标 Linux 板卡上编译 EGL pbuffer 冒烟示例。

常用本地验证顺序：

```bat
build_test.bat
build_dll.bat
build_exe.bat
```

## 示例脚本

当前示例脚本包括：

- `build_app_mode_exe.*`
- `build_async_exe.*`
- `build_audio_exe.*`
- `build_debug_overlay_exe.*`
- `build_gamepad_exe.*`
- `build_input_exe.*`
- `build_isometric_depth_exe.*`
- `build_platform_smoke_exe.*`
- `run_platform_smoke.*`
- `check_platform_smoke.bat`
- `build_board_linux_egl_exe.sh`
- `build_android_apk.*`
- `build_android_ndk.*`
- `check_platform_all.*`
- `check_platform_scaffold.*`
- `check_platform_results.*`
- `check_platform_smoke_log.*`
- `check_linux_x11_smoke.sh`
- `check_linux_egl_smoke.sh`
- `check_macos_smoke.sh`
- `check_script_docs.*`
- `check_docs.*`
- `record_platform_result.*`
- `run_android_apk.*`
- `build_egl_surfaceless_exe.sh`
- `check_board_linux_egl.sh`
- `build_ios_sim_exe.sh`
- `run_ios_sim_exe.sh`
- `build_linux_x11_exe.sh`
- `build_linux_egl_exe.sh`
- `build_macos_exe.sh`
- `build_perspective_quad_exe.*`
- `build_render_target_exe.*`
- `build_scene_exe.*`
- `build_shader_distortion_exe.*`
- `build_shader_material_exe.*`
- `build_shape_exe.*`
- `build_sprite_batch_exe.*`
- `build_text_exe.*`
- `build_text_input_exe.*`
- `build_texture_exe.*`
- `build_touch_exe.*`
- `build_web_exe.*`
- `serve_web_exe.*`
- `build_xui_bridge_exe.*`
- `build_xui_incubation_exe.*`

示例脚本应只负责编译对应示例，不承担批量测试职责。

## 新增脚本规则

新增示例时必须同时添加 Windows 与 Unix 两个脚本：

- `build_<example>_exe.bat`
- `build_<example>_exe.sh`

脚本内容应保持简单：

- 设置 `OUT_DIR=build`。
- 设置 `OUT` 为 `build/xge_<example>` 或 `build\xge_<example>.exe`。
- 设置 `SRC` 为 `xge.c examples/<example>/main.c`。
- 设置 `INC=-I.`。
- 使用项目统一 warning 参数。
- 编译失败时返回非 0。

## 批量验证建议

当前 Windows 下 GCC 并行编译多个大示例可能占用较多内存。建议每次并行运行 2 到 3 个脚本，或按以下顺序分组：

```bat
build_test.bat
build_dll.bat
build_exe.bat

build_shape_exe.bat
build_texture_exe.bat
build_text_exe.bat

build_render_target_exe.bat
build_shader_material_exe.bat
build_shader_distortion_exe.bat

build_sprite_batch_exe.bat
build_perspective_quad_exe.bat
build_isometric_depth_exe.bat

build_input_exe.bat
build_text_input_exe.bat
build_touch_exe.bat
build_gamepad_exe.bat

build_audio_exe.bat
build_async_exe.bat
build_debug_overlay_exe.bat
build_xui_incubation_exe.bat
build_xui_bridge_exe.bat
```

## 单头文件说明

仓库源码按模块拆分，便于阅读和维护。单头文件版本由 `singlehead/` 工具生成，不应把大量实现直接写回 `xge.h`。

后续单头文件生成器完善后，构建脚本应继续以源码拆分版为开发主路径，单头文件测试只验证集成产物。

## 平台验证结果

跨平台冒烟的实际执行结果统一记录到 `docs/平台后端验证结果.md`。脚本新增或验证流程变化时，应同步更新该文件中的执行脚本列表。

可用以下脚本检查当前验证结果是否足以关闭平台 spec 项：

```bat
check_platform_results.bat
```

Linux/macOS:

```sh
./check_platform_results.sh
```

目标平台执行完成后，可以用以下脚本记录结果：

```bat
record_platform_result.bat "Windows" 通过 --env "Windows 11 / GPU / gcc" --scripts "build_test.bat; build_platform_smoke_exe.bat" --summary "窗口和基础渲染正常"
```

Linux/macOS:

```sh
./record_platform_result.sh "Linux X11" 通过 --env "Ubuntu / X11 / Mesa" --scripts "./build_linux_x11_exe.sh" --summary "窗口、OpenGL、输入正常"
```

platform smoke 示例支持自动退出参数，便于在目标机采集日志：

```sh
./build/xge_linux_x11 --frames 180 > smoke.log
./check_platform_smoke_log.sh smoke.log --require-final
```

Windows:

```bat
build\xge_platform_smoke.exe --frames 180 > smoke.log
check_platform_smoke_log.bat smoke.log --require-final
```

也可以直接使用包装脚本：

```bat
check_platform_smoke.bat
```

Linux/macOS:

```sh
./check_linux_x11_smoke.sh
./check_linux_egl_smoke.sh
./check_macos_smoke.sh
```
