# XGE 平台后端验证结果

本文档用于记录各平台实际构建和冒烟结果。只有对应平台结果标记为 `通过`，并保留环境、脚本、观察结果和关键日志后，才能关闭 `XGE_V2_SPEC.md` 中的平台运行路径任务。

可运行以下脚本汇总当前验证状态：

```text
check_platform_results.bat
./check_platform_results.sh
```

可用以下脚本追加验证记录：

```text
record_platform_result.bat "Windows" 通过 --env "Windows 11 / GPU / gcc" --scripts "build_test.bat" --summary "通过"
./record_platform_result.sh "Linux X11" 通过 --env "Ubuntu / X11 / Mesa" --scripts "./build_linux_x11_exe.sh" --summary "通过"
```

可用以下脚本检查 platform smoke 日志是否包含 caps 与 runtime 快照：

```text
check_platform_smoke_log.bat smoke.log --require-final
./check_platform_smoke_log.sh smoke.log --require-final
```

状态：

- `未测`：尚未在目标平台执行。
- `通过`：完成构建、启动和 30 秒冒烟。
- `失败`：执行过，但存在构建、启动、渲染、输入或退出问题。
- `跳过`：当前机器缺少必要设备或工具链。

## Windows

状态：未测

环境：

```text
OS:
GPU:
Compiler:
```

执行脚本：

```text
check_platform_backends.bat
build_test.bat
build_platform_smoke_exe.bat
run_platform_smoke.bat
check_platform_smoke.bat
check_platform_smoke_log.bat
build_dll.bat
build_exe.bat
```

结果记录：

```text
构建：
窗口：
渲染：
输入：
日志：
结论：
```

## Linux X11

状态：未测

环境：

```text
Distro:
Session: X11
GPU/Driver:
Compiler:
```

执行脚本：

```text
./check_platform_backends.sh
./check_linux_x11_smoke.sh
./build_linux_x11_exe.sh
./run_platform_smoke.sh
./build_test.sh
./build_dll.sh
```

结果记录：

```text
构建：
窗口：
OpenGL:
输入：
日志：
结论：
```

## Linux X11/EGL

状态：未测

环境：

```text
Distro:
Session: X11
GPU/Driver:
EGL:
```

执行脚本：

```text
./build_linux_egl_exe.sh
./check_linux_egl_smoke.sh
```

结果记录：

```text
构建：
窗口：
GLES3/EGL：
输入：
日志：
结论：
```

## macOS

状态：未测

环境：

```text
macOS:
Mac model:
GPU:
Xcode/clang:
```

执行脚本：

```text
./check_platform_backends.sh
./check_macos_smoke.sh
./build_macos_exe.sh
```

结果记录：

```text
构建：
窗口：
OpenGL:
输入：
日志：
结论：
```

## Android

状态：未测

环境：

```text
Device/Emulator:
Android version/API:
GPU:
NDK:
Gradle:
```

执行脚本：

```text
./check_platform_backends.sh
./build_android_ndk.sh
./build_android_apk.sh
./run_android_apk.sh
```

结果记录：

```text
NDK 构建：
APK 构建：
安装启动：
GLES3:
Touch:
logcat:
结论：
```

## iOS

状态：未测

环境：

```text
macOS:
Xcode:
Simulator/Device:
iOS version:
```

执行脚本：

```text
./check_platform_backends.sh
./build_ios_sim_exe.sh
./run_ios_sim_exe.sh
```

结果记录：

```text
构建：
安装启动：
GLES3:
Touch/Text:
日志：
结论：
```

## Web/Emscripten

状态：未测

环境：

```text
OS:
emsdk/emcc:
Browser:
GPU:
```

执行脚本：

```text
./check_platform_backends.sh
./build_web_exe.sh
./serve_web_exe.sh
```

结果记录：

```text
构建：
启动：
WebGL2:
输入：
浏览器控制台：
结论：
```

## 板卡 Linux EGL pbuffer

状态：未测

环境：

```text
Board:
Distro:
GPU/Driver:
EGL extensions:
```

执行脚本：

```text
./build_board_linux_egl_exe.sh
./build/xge_board_linux_egl
./check_board_linux_egl.sh
```

结果记录：

```text
构建：
EGL pbuffer:
sLastStage:
iLastError:
日志：
结论：
```

## EGL surfaceless

状态：未测

环境：

```text
Board/OS:
GPU/Driver:
EGL extensions:
```

执行脚本：

```text
./build_egl_surfaceless_exe.sh
./build/xge_egl_surfaceless
./check_board_linux_egl.sh
```

结果记录：

```text
构建：
EGL_KHR_surfaceless_context:
sLastStage:
iLastError:
日志：
结论：
```
